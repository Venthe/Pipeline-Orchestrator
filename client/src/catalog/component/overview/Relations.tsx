import { Card, CardFooter, CardHeader, CardPreview } from "@fluentui/react-components";

export const Relations = () => (
    <Card>
        <CardHeader header={"Relations"} />
        --GRAPH--
        <CardFooter>View graph -></CardFooter>
    </Card>
)