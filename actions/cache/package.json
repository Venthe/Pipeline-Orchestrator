{
  "name": "@actions/cache",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "ncc": "ncc",
    "jest": "jest",
    "test": "jest --config=./jest.config.ts",
    "build": "npm run build:save && npm run build:restore",
    "build:save": "(mkdir -p dist/save || true) && rm -rf dist/save/* && ncc cache clean && ncc build src/save.ts --out dist/save --source-map --quiet --no-cache",
    "build:restore": "(mkdir -p dist/restore || true) && rm -rf dist/restore/* && ncc cache clean && ncc build src/restore.ts --out dist/restore --source-map --quiet --no-cache"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@pipeline/types": "*",
    "@types/jest": "^29.4.0",
    "@types/node": "^18.11.19",
    "@vercel/ncc": "^0.36.1",
    "jest": "^29.4.1",
    "ts-jest": "^29.0.5"
  },
  "dependencies": {
    "@pipeline/process": "*",
    "@pipeline/utilities": "*",
    "@pipeline/core": "*"
  }
}
