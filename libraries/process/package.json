{
  "name": "@pipeline/process",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "tsc": "tsc",
    "jest": "jest",
    "test": "jest --config=./jest.config.ts",
    "clean": "(mkdir -p dist || true) && rm -rf dist/*",
    "build": "npm run --quiet clean && tsc --project tsconfig.json",
    "build:watch": "npm run --quiet build -- --watch"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^18.11.19",
    "jest": "^29.4.1",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@pipeline/utilities": "*"
  }
}
